#include <stdio.h>
#include "main.h"

/**
  * main - Print program name
  * @argc: Number of arguments
  * @argv: Arguments
  *
  * Return: Always 0 (Success)
  */
int main(int argc, char *argv[])
{
	(int) argc;

	printf("%s\n", argv[0]);
	return (0);
}
